name: Build and deploy Backend Node.js app to Azure Web App

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies (Backend)
        working-directory: ./backend
        run: npm ci --production=false

      - name: Build Backend (if applicable)
        working-directory: ./backend
        run: npm run build --if-present

      - name: Prepare deployment package
        working-directory: ./backend
        run: |
          # Vérifier que les fichiers essentiels existent
          if [ ! -f "package.json" ]; then
            echo "❌ Erreur: package.json n'existe pas"
            exit 1
          fi
          
          if [ ! -f "server.js" ]; then
            echo "❌ Erreur: server.js n'existe pas"
            exit 1
          fi
          
          if [ ! -f "app.js" ]; then
            echo "❌ Erreur: app.js n'existe pas"
            exit 1
          fi
          
          # Vérifier que le script start est correct dans package.json
          if ! grep -q '"start":' package.json; then
            echo "❌ Erreur: script 'start' manquant dans package.json"
            exit 1
          fi
          
          # Créer un package propre sans node_modules (Azure les installera)
          rm -rf node_modules
          
          # Lister les fichiers qui seront déployés
          echo "📦 Fichiers à déployer:"
          ls -la
          
          # Vérifier le contenu de package.json
          echo "📄 Script start dans package.json:"
          grep '"start"' package.json
          
          echo "✅ Package préparé pour le déploiement"

      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: backend-app
          path: ./backend
          retention-days: 1

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: backend-app
          path: ./deploy-package

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_BACK_CLIENTID }}",
              "clientSecret": "${{ secrets.AZURE_BACK_CLIENTSECRET }}",
              "subscriptionId": "${{ secrets.AZURE_BACK_SUBSCRIPTION }}",
              "tenantId": "${{ secrets.AZURE_BACK_TENANTID }}"
            }

      - name: Verify App Service exists
        run: |
          APP_NAME="${{ secrets.AZURE_BACK_APP_NAME || 'Inventory-pro1' }}"
          RESOURCE_GROUP="${{ secrets.AZURE_BACK_RESOURCE_GROUP || 'Inventoryy22' }}"
          
          echo "Vérification de l'App Service: $APP_NAME dans le groupe $RESOURCE_GROUP"
          
          # Vérifier que l'App Service existe
          APP_EXISTS=$(az webapp show \
            --resource-group "$RESOURCE_GROUP" \
            --name "$APP_NAME" \
            --query "name" \
            -o tsv 2>/dev/null || echo "")
          
          if [ -z "$APP_EXISTS" ]; then
            echo "❌ Erreur: L'App Service '$APP_NAME' n'existe pas dans le groupe de ressources '$RESOURCE_GROUP'"
            echo "Vérifiez le nom de l'App Service dans Azure Portal ou créez-le avec:"
            echo "  az webapp create --resource-group $RESOURCE_GROUP --plan ProjetStock-Plan --name $APP_NAME --runtime 'NODE:20-lts'"
            exit 1
          fi
          
          echo "✅ App Service trouvé: $APP_EXISTS"
          
          # Afficher les informations de l'App Service
          echo "📋 Informations de l'App Service:"
          az webapp show \
            --resource-group "$RESOURCE_GROUP" \
            --name "$APP_NAME" \
            --query "{Name:name, State:state, Location:location, Runtime:siteConfig.linuxFxVersion}" \
            -o table

      - name: Verify deployment package
        run: |
          echo "📦 Vérification du package de déploiement:"
          ls -la ./deploy-package/
          echo ""
          echo "📄 Contenu de package.json:"
          cat ./deploy-package/package.json
          echo ""
          if [ -f "./deploy-package/server.js" ]; then
            echo "✅ server.js présent"
          else
            echo "❌ server.js manquant"
            exit 1
          fi
          if [ -f "./deploy-package/app.js" ]; then
            echo "✅ app.js présent"
          else
            echo "❌ app.js manquant"
            exit 1
          fi

      - name: Deploy to Azure Web App (Backend)
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_BACK_APP_NAME || 'Inventory-pro1' }}
          package: ./deploy-package
          slot-name: 'production'
          type: zip
          
      - name: Configure startup command and settings via Azure CLI
        run: |
          APP_NAME="${{ secrets.AZURE_BACK_APP_NAME || 'Inventory-pro1' }}"
          RESOURCE_GROUP="${{ secrets.AZURE_BACK_RESOURCE_GROUP || 'Inventoryy22' }}"
          
          echo "Configuration de la commande de démarrage pour $APP_NAME dans le groupe $RESOURCE_GROUP"
          
          # Configurer la commande de démarrage
          az webapp config set \
            --resource-group "$RESOURCE_GROUP" \
            --name "$APP_NAME" \
            --startup-file "npm start" \
            --output none
          
          echo "✅ Commande de démarrage configurée"
          
          # Vérifier la configuration
          echo "📋 Configuration actuelle:"
          az webapp config show \
            --resource-group "$RESOURCE_GROUP" \
            --name "$APP_NAME" \
            --query "{StartupFile:linuxFxVersion, AlwaysOn:alwaysOn}" \
            -o table
          
          # Redémarrer l'application pour appliquer les changements
          echo "🔄 Redémarrage de l'application..."
          az webapp restart \
            --resource-group "$RESOURCE_GROUP" \
            --name "$APP_NAME" \
            --output none
          
          echo "✅ Application redémarrée"
          
      - name: Verify deployment
        run: |
          echo "✅ Déploiement terminé"
          echo "URL de l'API: https://${{ secrets.AZURE_BACK_APP_NAME || 'Inventory-pro1' }}.azurewebsites.net/api"
